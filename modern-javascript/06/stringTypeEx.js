// 문자열 타입
var string;
string = '문자열'; // 작은 따음표
string = "문자열"; // 큰 따음표
string = `문자열`; // 백틱 (ES6)
string = '작은 따음표로 감싼 문자열 내의 "큰따음표"는 문자열로 인식된다.';
string = "큰 따음표로 감싼 문자열 내의 '작은따음표'는 문자열로 인식된다.";

console.log(string);